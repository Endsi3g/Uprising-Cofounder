# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [2.3.0] - 2026-03-12

### Added

- **Multi-Provider LLM Support**: Added `LLM_PROVIDER` env var supporting `gemini`, `groq`, `openrouter`, and `local` (Ollama). Deploy with any cloud LLM provider without modifying code.
- **Interactive Deploy Script**: `deploy.ps1` now features a full interactive menu (Local Docker + Ollama / Groq / OpenRouter / Render.com).
- **Render.com Deployment**: Added `render.yaml` Blueprint for one-click cloud deployment (backend + static frontend + managed Postgres) and `Dockerfile.render` (lean image without Ollama).
- **BullMQ + Upstash Redis Message Queue**: Asynchronous job processing via `queueService.ts` with dedicated email and report workers.
- **PostHog Product Analytics**: Full `posthog-js` (client) + `posthog-node` (server) integration for pageview tracking and feature analytics.
- **Vercel Deployment**: Added `vercel.json` for zero-config Vercel frontend deployment with `/api` proxy to Render backend.
- **Supabase Database Integration**: `DATABASE_URL` now supports external Supabase PostgreSQL as primary database.

### Changed

- **Docker Entrypoint**: Ollama now only starts when `LLM_PROVIDER=local`, reducing startup time by ~60% for cloud LLM modes.
- **docker-compose.yml**: Full env var passthrough for all LLM providers and optional services.
- **`.env.example`**: Expanded with all new service variables (Supabase, Upstash, PostHog, Groq, OpenRouter).

---

## [2.2.1] - 2026-03-10

### Added

- **Developer Skip Login**: Added an "Accès Développeur" button on the login page to bypass authentication during development and testing.

### Fixed

- **Frontend Loading Hang**: Resolved a critical issue where the application would hang on the loading screen if the backend was unresponsive or in the process of restarting.

## [2.2.0] - 2026-03-10

### Added

- **Premium Documentation**: Completely redesigned the documentation page with a modern, high-end aesthetic, utilizing Framer Motion for interactive transitions.
- **Frontend-Only Launch Script**: Created `scripts/launch-frontend.ps1` and `dev:ui` NPM script to allow running the frontend independently with a built-in API proxy to the backend.

### Improved

- **UI Enhancements**: Globally hid scrollbars while maintaining full scrolling functionality and added smooth-scrolling behavior for a more premium experience.

### Fixed

- **Prisma Integration**: Resolved Prisma client export errors by restoring stable dependencies and forcing client re-generation.
- **Automated Testing**: Fixed Playwright smoke tests to match the updated application title and branding.

## [2.1.1] - 2026-03-09

### Changed

- **Build Optimizations**: Implemented multi-stage Docker builds for the backend.
- **Ollama Deployment**: Optimized Docker caching by moving Ollama installation to the final stage of the build process.

## [2.1.0] - 2026-03-08

### Added

- **PostgreSQL Migration**: Fully migrated DB from `better-sqlite3` to PostgreSQL using Prisma ORM.
- **Enterprise Security**: Added MFA (Multi-Factor Authentication), Email Verification, and Password Reset flows.
- **Dockerization**: Full stack Dockerization with persistent storage and integrated Ollama.
- **CI/CD Pipeline**: Enhanced GitHub Actions with Docker build validation and automated Prisma generation.
- **Service Layer**: Robust service architecture for AI (Gemini), SMS (Twilio), Voice (Bland), and CRM (Twenty).

### Changed

- **Codebase Reorganization**: Cleaned up the root directory and moved the backend entry point to `server/index.ts`.
- **API Refactoring**: All endpoints rewritten to support asynchronous Prisma queries.
- **Frontend Enhancements**: Updated user settings and account management to support new security features.

### Fixed

- Prisma Client generation issues and TypeScript synchronization between frontend and backend.
- Root directory clutter (removed legacy log and temporary files).

---
*Generated by Uprising AI Agent.*
